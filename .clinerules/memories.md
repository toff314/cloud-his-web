# Development Tools
- User prefers configuring Vite dev server with host 0.0.0.0 to allow external browser access.
- User prefers skipping build/packaging steps and running the project startup directly via npm commands.
